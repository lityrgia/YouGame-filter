# Contributing

1. Create a branch from `main`.
2. Keep changes focused and avoid adding third-party code without its license.
3. Run `npm run check` before opening a pull request.
4. Describe the affected YouGame page and how to reproduce the behaviour.

Release archives are generated with `npm run package:all` and must not be committed.
